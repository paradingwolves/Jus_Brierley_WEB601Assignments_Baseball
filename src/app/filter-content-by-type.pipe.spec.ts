import { FilterContentByTypePipe } from './filter-content-by-type.pipe';


describe('FilterContentByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterContentByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
